# Stanford AI API Gateway key (required for AI assistant features)
STANFORD_API_KEY=your_api_key_here

# Optional: Override the default LLM model (default: claude-4-sonnet)
# STANFORD_MODEL=claude-4-sonnet

# Optional: EPICS simulation mode (default: true)
# EIWYG_SIM_MODE=true

# Deployment mode: "dev" (default) or "production"
# In production mode, PostgreSQL is required — the app won't start without it.
# In dev mode, SQLite is used when no PG vars are set.
# EIWYG_ENV=production

# Optional: Base path for subpath deployment (e.g., /eiwyg)
# When set, all routes are served under this prefix.
# Default is empty (app served at root "/").
# EIWYG_BASE_PATH=/eiwyg

# PostgreSQL connection (required when EIWYG_ENV=production, optional in dev)
# Set these to use postgres instead of the default SQLite database.
# Alternatively, set DATABASE_URL=postgresql://user:pass@host:5432/dbname
# PGHOST=localhost
# PGPORT=5432
# PGUSER=eiwyg
# PGPASSWORD=secret
# PGDATABASE=eiwyg
